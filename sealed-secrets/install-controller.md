# Install Sealed Secrets Controller

Install the Bitnami Sealed Secrets controller in your cluster using the official installation method from the project repository. The controller stores the private key inside the cluster and uses it to decrypt sealed secrets.

After installation, verify that the controller is running before creating any sealed secrets.
